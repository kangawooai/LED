import Error404Split from "@/components/blocks/error/error-404-split";

export default function NotFound() {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <Error404Split />
    </div>
  );
}
